{
  "name": "fintech-expenses-challenge",
  "version": "1.0.0",
  "author": "Guilherme Gonçalves",
  "license": "MIT",
  "description": "Gestão Financeira Corporativa — NestJS + React.js",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run start:dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "migration:run": "npm run migration:run --prefix backend",
    "migration:revert": "npm run migration:revert --prefix backend",
    "test": "npm run test --prefix backend"
  }
}
